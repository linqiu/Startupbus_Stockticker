INSTALLATION:
1.) copy into wp-content/plugins

needs jquery!

release notes:
- added custom bus_id classes for styling
- programmitically adding custom JS so it doesn't affect where you place the widget
- changed all $ to jQuery
- added custom title field for widget
- JSON feed coming from startupbus (special shout out to Mr. Leonardo for the JSONP tip)
- fixed mouse over algorithm so its speed is even
- speed and width of scrolling adjusts automatically for different number of companies
- added custom width field in the widget on wp-admin side

FEATURES NEEDED:
1.) add field in widget to show individual stock (will need GET access to JSON feed)
2.) add field in widget to show tribe stock (will need GET access to JSON feed)

3.) links added to each company stock (HOLD: need actual urls)
4.) show whether the stocks are doing well or poorly (HOLD: waiting on api feature)
